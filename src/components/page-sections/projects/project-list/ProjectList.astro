---
import CustomSection from "@builders/custom-section/CustomSection.astro";
import Heading from "@core-elements/heading/Heading.astro";
import SimpleText from "@core-elements/simple-text/SimpleText.astro";
import Text from "@core-elements/text/Text.astro";
import ProjectItem from "@projects/project-list/ProjectItem.astro";
import Grid from "@wrappers/grid/Grid.astro";

type ProjectProps = Record<string, unknown>;

const {
  eyebrow = "",
  heading = "",
  subtext = "",
  features = [],
  colorScheme = "inherit",
  backgroundColor,
  paddingVertical = "4xl",
  class: className,
  useDefaultEditableBinding = false,
  _component,
  ...htmlAttributes
} = Astro.props;
---

<CustomSection
  class:list={["project-list", className]}
  maxContentWidth="2xl"
  paddingHorizontal="lg"
  paddingVertical={paddingVertical}
  colorScheme={colorScheme}
  backgroundColor={backgroundColor}
  useDefaultEditableBinding={useDefaultEditableBinding}
  {...htmlAttributes}
>
  <SimpleText
    alignX="center"
    class="eyebrow"
    data-prop="eyebrow"
    text={eyebrow}
  />
  <Heading
    level="h2"
    size="lg"
    alignX="center"
    data-prop="heading"
  >
    {heading}
  </Heading>
  <Text
    class="project-list-subtext"
    alignX="center"
    data-prop="subtext"
    text={subtext}
  />
  <Grid
    gap="lg"
    minItemWidth="360"
    maxItemWidth="360"
    class="project-list-grid"
    data-children-prop="features"
  >
    {
      features.map((feature: ProjectProps) => (
        <ProjectItem
          data-id="page-sections/features/project-list/feature-item"
          data-editable="array-item"
          {...feature}
        />
      ))
    }
  </Grid>
</CustomSection>

<style>
  @layer page-sections {
    :global(.project-list-grid) {
      margin-top: var(--spacing-2xl);
    }
  }
</style>
